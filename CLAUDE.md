# math-teacher 작업 지침

## 오류·개선 요청은 노션이 단일 소스다

보고된 오류와 개선 요청은 노션 **오류 정리 DB** 에 모인다. 이 레포의 이슈 트래커라고
보면 된다.

- 워크스페이스: `Math Teacher` (Notion MCP 로 접근)
- 상위 페이지: **오류 정리**
- DB data source: `collection://2242da4f-3b47-8299-9911-870c47081d52`
- 항목 구조(템플릿 "에러 기록 양식"): `에러 파일명` / `에러 이미지` / `에러내용 상세` / `처리방안`

### 처리 순서

1. **착수 전에 해당 항목을 읽는다.** `notion-fetch` 로 본문을 가져온다. `에러 파일명` 에
   적힌 파일이 재현 조건이므로 건너뛰지 않는다.
2. **`에러내용 상세` 가 비어 있으면 처리하지 않는다 — 처리보류로 두고 보고한다.**
   제목만 보고 요구를 추측해 구현하면 안 된다. 추측이 맞을 수도 있지만, 틀리면 엉뚱한
   것을 만들어 배포까지 해 버린다. 되돌리는 비용이 물어보는 비용보다 훨씬 크다.

   보류 시에는 어떤 항목이 왜 보류인지, 무엇을 채워 주면 착수할 수 있는지 사용자에게
   보고한다. 노션 항목의 `처리방안` 에도 보류 사유를 남겨, 보고자가 내용을 채울 계기를
   만든다(코드는 건드리지 않는다).

   예외는 하나다 — 사용자가 그 항목을 **명시적으로 지정해** 진행을 지시한 경우. 그때도
   먼저 제목을 어떻게 읽었는지 해석을 밝히고 확인을 받은 뒤 착수한다.
3. 고친다. 계산 로직은 순수 함수로 빼서 단위 테스트로 굳히는 것이 이 레포의 관례다
   (예: `app/web/src/components/tree/selection.ts` + `selection.test.ts`).
4. **처리 후 반드시 그 항목의 `처리방안` 에 기록한다.** 이것을 빠뜨리고 작업을 끝내지
   않는다. 보고자가 노션만 보고도 무엇이 어떻게 됐는지 알 수 있어야 한다.

   ```
   notion-update-page
     page_id: <해당 항목>
     command: insert_content
     position: {"type": "end"}
   ```

   기록에 담을 것:

   - **원인** — 증상이 아니라 왜 그랬는지. 보고 문구의 어느 부분이 이것인지 짚어준다.
   - **처리한 것** — 키/버튼 단위로 무엇이 어떻게 동작하는지. 설계 판단(왜 그렇게 했는지)도 한 줄씩.
   - **이번에 하지 않은 것과 그 이유** — 범위를 줄였으면 반드시 남긴다. 조용히 빼면
     보고자는 다 된 줄 안다.
   - **검증** — 테스트 개수, 빌드, 배포 상태.
   - **커밋 해시와 배포 ID.**

5. **처리를 끝냈으면 `개발` 체크박스를 켠다.** DB 속성은 `개발`·`개발의견`·`완료`
   세 개의 체크박스다(2026-08-22 확인 — 그전에는 `개발`·`완료` 둘, 더 전에는 `Todo` 하나였다).

   - **`개발`** — 개발자가 켠다. **배포까지 확인한 뒤** 켠다. 커밋만 하고 켜지
     않는다 — 배포가 실패하면 고쳐진 것이 아니다.
   - **`개발의견`** — 개발자가 켠다. **고치기 전에 의견을 물어야 할 때** 쓴다.
     조사 결과·선택지·착수 조건을 `처리방안` 에 정리해 올리고 이 칸을 켜면,
     보고자가 "답을 기다리는 항목" 으로 알아본다(사용자 확인 — 2026-08-22).
     즉 `개발` 은 "다 됐다", `개발의견` 은 "답을 주셔야 진행된다" 는 뜻이다.
     답을 받아 처리까지 끝나면 `개발` 을 켜면 된다.
   - **`완료`** — 보고자 몫이다. **켜지 않는다.** 실제로 해소됐는지는 보고한
     사람이 확인할 일이고, 개발자가 대신 판단할 수 없다.

   **부분 처리는 켜지 않는다.** 그 항목에 아직 처리하지 못한 요구가 남아 있으면
   `개발` 을 비워 두고, 무엇이 남았는지 `처리방안` 에 적는다. 켜 버리면 남은
   일이 목록에서 사라진다. 판정 기준은 "보고된 증상이 해소되었고 배포까지
   확인했는가" 다.

   처리보류(본문 없음)도 당연히 켜지 않는다.

## 기능 요청은 별도 DB 다

버그가 아닌 **기능 제안**은 노션 `기능 요청` DB 로 온다. 오류 정리 DB 와 진행 표시
방식도, 내가 쓰는 칸도 다르다. 헷갈리면 엉뚱한 칸에 쓴다.

- data source: `collection://3c32da4f-3b47-805a-876c-000b935ef306`
- 섹션: `요청 내용 (성락윤)` / `기능 정리 및 가능 여부 (이현우)` / `처리 내용 (이현우)`
  — **담당이 제목에 박혀 있다.** 요청자 칸은 건드리지 말고, 내 칸에만 쓴다.
- `상태`(status): `신규` → `검토 중` → `계획됨` → `개발 중` → `출시됨` / `거부됨`
- 그 외: `우선순위`(높음·보통·낮음), `예상되는 노력`(S·M·L·XL), `제품 영역`(모바일·성장·AI·기타)

### 처리 순서

1. 착수 전 요청 내용을 읽고, **`기능 정리 및 가능 여부` 에 가능 여부·방법·확인이
   필요한 점을 적는다.** 여기서 판단이 갈리는 지점은 선택지로 제시한다.
2. `상태` 를 `개발 중` 으로, `예상되는 노력`·`제품 영역` 을 채운다.
3. 고친다.
4. **배포까지 확인한 뒤** `처리 내용` 에 사용자가 화면에서 무엇을 보게 되는지 적고,
   `상태` 를 `출시됨` 으로 바꾼다. 배포 전에 `출시됨` 으로 바꾸지 않는다.
5. 거부해야 하면 `거부됨` + `기능 정리 및 가능 여부` 에 이유를 적는다. 조용히 두지 않는다.

## 배포

프론트와 백엔드가 배포 경로도 소요 시간도 다르다. **섞어서 한 커밋에 올리지 않는다.**

| 대상 | 경로 | 소요 |
| --- | --- | --- |
| `app/web/**` | GitHub push → Vercel 자동배포 | 1~2분 |
| `app/core/**` | GitHub push → CI → GHCR → k8s-helm 태그 커밋 → ArgoCD | 4~10분 |

- Vercel 프로젝트의 **Root Directory 는 `app/web`** 이다(빌드가 그 안에서 돈다 — 로그로 확인).
- **Skip deployments 토글은 켜 두었지만 실제로 스킵되지 않는다.** 루트의 `CLAUDE.md` 만
  바꾼 커밋(`1452cf9`)에서도 전체 빌드가 돌았다. *추측:* 같은 설정 섹션의 "Include files
  outside the root directory in the Build Step" 이 Enabled 라 빌드 입력이 레포 전체가
  되기 때문. **확인되기 전까지는 어떤 커밋이든 프론트 배포가 돈다고 가정할 것.**
- 백엔드 주소는 런타임이 아니라 **빌드 시점** `NEXT_PUBLIC_API_BASE` 로 들어간다.
  값은 Vercel 프로젝트 환경변수에 있다. 로컬 `app/web/.env.production` 은 gitignore 라
  Git 배포 경로에는 올라가지 않는다.
- 프론트와 백엔드를 함께 바꿨다면 **`app/core` 를 먼저 올리고 ArgoCD 반영을 확인한 뒤**
  `app/web` 을 올린다. 반대로 하면 새 프론트가 아직 없는 API 를 불러 앱이 깨진다.

### 배포 검증

- 검증에 쓸 공개 URL 은 **`https://web-five-peach-4vf3xgcbap.vercel.app/`** 다.
  프로젝트명 기반 `math-teacher-jenu8628s-projects.vercel.app` 은 Vercel SSO 로 302 가 나서
  외부에서 못 읽는다(같은 배포인데 도메인에 따라 갈린다).
- 배포 성공 여부는 HTTP 200 으로 판단하지 않는다. 옛 번들이 그대로 떠 있어도 200 이다.
  **프로덕션 번들에 새 코드 마커가 들어갔는지 grep 으로 확인**한다.
- 청크 목록을 뽑는 정규식에 **`/` 를 반드시 넣는다** — `/_next/static/[a-zA-Z0-9._/-]+\.js`.
  문자클래스에 `/` 가 없으면 앱 코드 대부분이 든 `app/page-<hash>.js` 를 놓쳐서,
  배포가 정상인데도 실패로 오판한다(실제로 겪음).

## 테스트

```bash
cd app/web
npx vitest run          # 전체
npx tsc --noEmit        # 타입 (noUncheckedIndexedAccess 켜져 있음 — 배열 인덱싱은 undefined 를 포함한다)
npm run build           # 프로덕션 빌드 (output: 'export' 정적 export)
```

커밋 전에 세 가지를 모두 통과시킨다.
